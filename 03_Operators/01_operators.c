#include <stdio.h>
int main(){
    int a = 5;
    int b = 16;
    int c = a + b;
    printf("The value of a is %d The value of b is %d and there sum is %d \n", a, b, c);
    printf(" The remainder when b is divided by a %d", b%a);
    return 0;
}