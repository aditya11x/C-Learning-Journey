#include <stdio.h>
int main(){
    int Age = 16;
    float height = 5.6;
    char grade = 'A';

    printf("Age is %d \n" , Age); // %d for integer
    printf("Height is %.1f \n", height); // %ffor decimal numbers, %.1f the number tells the number of decimal places after number
    printf("Grade is %c \n", grade);// for characters
    return 0;
}


